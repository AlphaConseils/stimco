<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <record id="view_partner_form_stimco_contacts" model="ir.ui.view">
        <field name="name">view.partner.form.stimco.contacts</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <field name="nif" string="NIF" attrs="{'invisible': [('company_type', '!=', 'company')]}"/>
                <field name="stat" string="STAT" attrs="{'invisible': [('company_type', '!=', 'company')]}"/>
                <field name="rcs" string="RCS" attrs="{'invisible': [('company_type', '!=', 'company')]}"/>
            </field>
            <field name="vat" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
        </field>
    </record>
</odoo>